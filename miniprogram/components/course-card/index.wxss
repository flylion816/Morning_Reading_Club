.course-card {
  background: linear-gradient(135deg, #ffffff 0%, #f8fbff 100%);
  border-radius: 16rpx;
  padding: 28rpx;
  margin-bottom: 0;
  display: flex;
  gap: 24rpx;
  transition: all 0.3s;
}

.course-card.pending {
  background: linear-gradient(135deg, var(--theme-primary-tint) 0%, #f8fbff 100%);
}

/* 左侧缩略图 */
.course-thumb {
  width: 120rpx;
  height: 150rpx;
  flex-shrink: 0;
  background: linear-gradient(135deg, var(--theme-primary) 0%, var(--theme-primary-dark) 100%);
  border-radius: 12rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.thumb-icon {
  font-size: 64rpx;
}

/* 右侧内容区 */
.course-info {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  min-width: 0;
}

.course-title {
  font-weight: bold;
  color: #333333;
  font-size: 30rpx;
  margin-bottom: 8rpx;
  line-height: 1.4;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.course-meta {
  font-size: 24rpx;
  color: #999999;
  margin-bottom: 16rpx;
}

/* 进度条区域 */
.course-progress {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.progress-bar {
  height: 8rpx;
  background: linear-gradient(90deg, #e2e8f0 0%, #cbd5e0 100%);
  border-radius: 4rpx;
  overflow: hidden;
  box-shadow: inset 0 2rpx 4rpx rgba(0, 0, 0, 0.05);
}

.progress-fill {
  height: 100%;
  border-radius: 4rpx;
  transition: width 0.3s ease;
}

.progress-fill-active {
  background: linear-gradient(90deg, var(--theme-primary) 0%, var(--theme-primary-dark) 100%);
  box-shadow: 0 2rpx 6rpx var(--theme-primary-shadow);
}

.progress-fill-done {
  background: #cbd5e0;
}

.course-actions {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.course-actions.period-mode {
  justify-content: flex-end;
}

.status-text {
  font-size: 24rpx;
  color: #666666;
  flex: 1;
}

.status-text.status-right {
  flex: none;
  text-align: right;
  font-weight: 500;
  color: #8c8c8c;
}

.status-text.status-not_started,
.status-text.status-ongoing {
  color: var(--theme-primary);
}

.action-btn {
  padding: 8rpx 24rpx;
  border-radius: 32rpx;
  font-size: 24rpx;
  border: none;
  transition: all 0.3s;
  flex-shrink: 0;
  margin-left: auto;
}

.action-btn.primary {
  background: var(--theme-primary);
  color: #ffffff;
}

.action-btn.secondary {
  background: transparent;
  color: var(--theme-primary);
  border: 2rpx solid var(--theme-primary);
}

.action-btn.secondary::after {
  border: none;
}
