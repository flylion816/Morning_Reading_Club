#!/bin/bash
# 启动后端服务脚本
# 用于启动 Node.js 后端服务

set -e

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m'

echo -e "${BLUE}════════════════════════════════════════════${NC}"
echo -e "${BLUE}🚀 启动后端服务${NC}"
echo -e "${BLUE}════════════════════════════════════════════${NC}"
echo ""

# 检查是否在项目根目录
if [ ! -d "backend" ]; then
    echo -e "${RED}❌ 错误: 未找到 backend 目录${NC}"
    echo "请在项目根目录运行此脚本"
    exit 1
fi

# 清理之前的进程
echo -e "${YELLOW}🧹 清理之前的 Node 进程...${NC}"
pkill -f "node.*backend" 2>/dev/null || true
sleep 1

# 进入后端目录
cd backend

# 检查是否已安装依赖
if [ ! -d "node_modules" ]; then
    echo -e "${YELLOW}📦 安装依赖...${NC}"
    npm install
    echo ""
fi

echo -e "${YELLOW}⏳ 启动后端服务...${NC}"
npm run dev

# 不会执行到这里，但为了完整性保留
exit 0
