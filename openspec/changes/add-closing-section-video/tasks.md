## 1. 规格确认
- [x] 1.1 实现前确认本变更方案已通过审批。

## 2. 后端
- [x] 2.1 在 `Section` 模型增加可选 `closingVideo` 子对象字段。
- [x] 2.2 增加管理员鉴权的视频上传接口，限制视频类型、大小和租户目录。
- [x] 2.3 复用或扩展图片上传能力，支持后台上传视频首帧封面图。
- [x] 2.4 创建、更新、详情和列表接口透传 `closingVideo`，并支持清空该字段。
- [x] 2.5 增加后端单元测试或接口测试，覆盖上传限制、课节保存和详情返回。

## 3. 管理后台
- [x] 3.1 扩展 `Section` 和课节表单类型，加入 `closingVideo` 字段。
- [x] 3.2 在课节编辑弹窗扩展信息区增加“结营视频”上传、预览、更换、删除控件。
- [x] 3.3 上传视频时生成首帧封面并上传，失败时保留视频并给出封面回退提示。
- [x] 3.4 保存课节时提交 `closingVideo`，编辑已有课节时回显视频和封面。
- [x] 3.5 增加管理后台 Vitest，覆盖字段回显、保存 payload 和删除行为。

## 4. 小程序
- [x] 4.1 课程详情归一化 `closingVideo`，把相对地址转成完整 URL。
- [x] 4.2 在课程详情页展示“结营视频”板块，支持原生 video 播放和封面 poster。
- [x] 4.3 增加视频分享按钮，分享路径携带 `anchor=closingVideo` 并使用首帧封面。
- [x] 4.4 普通结营词分享在存在 `closingVideo.coverUrl` 时使用该封面作为分享卡片图片。
- [x] 4.5 支持从分享进入后滚动到结营视频板块。
- [x] 4.6 增加小程序 Jest，覆盖视频板块展示、分享模式和封面优先级。

## 5. 验证
- [x] 5.1 运行后端相关测试。
- [x] 5.2 运行管理后台相关测试。
- [x] 5.3 运行小程序课程详情相关测试。
- [ ] 5.4 在微信开发者工具人工验证：视频可播放、分享好友卡片封面为首帧、无视频课节不展示板块。
